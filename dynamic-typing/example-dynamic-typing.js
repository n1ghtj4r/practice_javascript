let example = "Hello";
console.log(example);

//changing it in to number or int value

example = 42;
console.log(example);

//another example of creating a variable with a type set to number then changing it to later be of type string

let data = 100;  // Initially a number
console.log(data);

data = "New data";  // Dynamically changes to a string
console.log(data);